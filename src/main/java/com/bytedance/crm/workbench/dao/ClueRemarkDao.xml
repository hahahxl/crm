<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bytedance.crm.workbench.dao.ClueRemarkDao">

    <select id="getClueRemarkListByClueId" resultType="com.bytedance.crm.workbench.domain.ClueRemark">
        select * from tbl_clue_remark where clueId =#{clueId}

    </select>

    <delete id="delete">
        delete FROM tbl_clue_remark where id=#{id}
    </delete>
</mapper>

































